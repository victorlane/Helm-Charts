Thank you for installing Operately!

1. Access
   - Service: {{ include "operately.fullname" . }} on port {{ .Values.service.port }}
   - Ingress: {{- if .Values.ingress.enabled }} enabled; host(s): {{- range .Values.ingress.hosts }} {{ .host }}{{ end }} {{- else }} disabled {{- end }}

2. Migrations
   - A pre-install/pre-upgrade Job runs DB create + migrate.

3. Database
   - Using {{- if .Values.postgresql.enabled }} in-cluster PostgreSQL (Bitnami) {{- else if .Values.externalPostgresql.enabled }} external database {{- else }} no database configured {{- end }}.

4. Persistence
   - Media volume {{- if .Values.persistence.enabled }} PVC {{ include "operately.fullname" . }}-media ({{ .Values.persistence.size }}, class {{ .Values.persistence.storageClassName | default "default" }}) {{- else }} emptyDir (ephemeral) {{- end }}.
