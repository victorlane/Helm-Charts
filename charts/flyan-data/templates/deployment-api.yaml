apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-api
  labels:
    {{- include "flyan-data.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  replicas: {{ .Values.flyanData.api.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Chart.Name }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        {{- include "flyan-data.labels" . | nindent 8 }}
        app.kubernetes.io/component: api
      annotations:
        {{- if .Values.flyanData.metrics.enabled }}
        {{- toYaml .Values.flyanData.metrics.podAnnotations | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.flyanData.image.pullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      {{- with .Values.flyanData.api.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.flyanData.api.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: migrate
          image: {{ include "flyan-data.image" . }}
          imagePullPolicy: {{ .Values.flyanData.image.pullPolicy }}
          command: ["python", "manage.py", "migrate", "--noinput"]
          env:
            {{- include "flyan-data.env" . | nindent 12 }}
      containers:
        - name: api
          image: {{ include "flyan-data.image" . }}
          imagePullPolicy: {{ .Values.flyanData.image.pullPolicy }}
          args:
            - "uvicorn"
            - "flyan_data.asgi:application"
            - "--host=0.0.0.0"
            - "--port={{ .Values.flyanData.api.port }}"
            - "--workers={{ .Values.flyanData.api.workers }}"
            - "--proxy-headers"
            - "--forwarded-allow-ips=*"
            - "--access-log"
          ports:
            - name: http
              containerPort: {{ .Values.flyanData.api.port }}
              protocol: TCP
          env:
            {{- include "flyan-data.env" . | nindent 12 }}
            {{- range $k, $v := .Values.flyanData.api.extraEnv }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /health/
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
          resources:
            {{- toYaml .Values.flyanData.api.resources | nindent 12 }}
