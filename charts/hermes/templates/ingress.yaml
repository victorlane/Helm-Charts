{{- if .Values.hermes.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  labels:
    {{- include "hermes.labels" . | nindent 4 }}
  {{- with .Values.hermes.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.hermes.ingress.className }}
  ingressClassName: {{ . | quote }}
  {{- end }}
  {{- with .Values.hermes.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.hermes.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $.Release.Name }}-api
                port:
                  number: 80
          {{- end }}
    {{- end }}
{{- end }}
