{{- $defaultResources := .Values.hermes.cron.resources }}
{{- $contexts := list
    (dict "name" "sync-network"             "cmd" "sync_network"             "schedule" .Values.hermes.cron.syncNetwork.schedule             "resources" (.Values.hermes.cron.syncNetwork.resources             | default $defaultResources))
    (dict "name" "plan-full-sweep"          "cmd" "plan_full_sweep"          "schedule" .Values.hermes.cron.planFullSweep.schedule          "resources" (.Values.hermes.cron.planFullSweep.resources          | default $defaultResources))
    (dict "name" "plan-hot-routes"          "cmd" "plan_hot_routes"          "schedule" .Values.hermes.cron.planHotRoutes.schedule          "resources" (.Values.hermes.cron.planHotRoutes.resources          | default $defaultResources))
    (dict "name" "plan-continuous"          "cmd" "plan_continuous"          "schedule" .Values.hermes.cron.planContinuous.schedule          "resources" (.Values.hermes.cron.planContinuous.resources          | default $defaultResources))
    (dict "name" "test-proxies"             "cmd" "test_proxies"             "schedule" .Values.hermes.cron.testProxies.schedule             "resources" (.Values.hermes.cron.testProxies.resources             | default $defaultResources))
    (dict "name" "detect-anomalies"         "cmd" "detect_anomalies"         "schedule" .Values.hermes.cron.detectAnomalies.schedule         "resources" (.Values.hermes.cron.detectAnomalies.resources         | default $defaultResources))
    (dict "name" "refresh-current-cheapest" "cmd" "refresh_current_cheapest" "schedule" .Values.hermes.cron.refreshCurrentCheapest.schedule "resources" (.Values.hermes.cron.refreshCurrentCheapest.resources | default $defaultResources))
}}
{{- range $ctx := $contexts }}
{{- if $ctx.schedule }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ $.Release.Name }}-{{ $ctx.name }}
  labels:
    {{- include "hermes.labels" $ | nindent 4 }}
    app.kubernetes.io/component: cron
spec:
  schedule: {{ $ctx.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  startingDeadlineSeconds: 300
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 1800
      template:
        metadata:
          labels:
            {{- include "hermes.labels" $ | nindent 12 }}
            app.kubernetes.io/component: cron
            cron.hermes/job: {{ $ctx.name }}
        spec:
          restartPolicy: Never
          {{- with $.Values.hermes.image.pullSecrets }}
          imagePullSecrets:
            {{- range . }}
            - name: {{ . }}
            {{- end }}
          {{- end }}
          containers:
            - name: {{ $ctx.name }}
              image: {{ include "hermes.image" $ }}
              imagePullPolicy: {{ $.Values.hermes.image.pullPolicy }}
              command:
                - python
                - manage.py
                - {{ $ctx.cmd }}
              env:
                {{- include "hermes.env" $ | nindent 16 }}
              resources:
                {{- toYaml $ctx.resources | nindent 16 }}
{{- end }}
{{- end }}
